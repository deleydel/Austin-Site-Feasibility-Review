# Raw data is too large for GitHub (several files exceed the 100 MB hard
# limit) — download it via the link in data/README.md. The processed datasets
# and the vector index ARE committed so teammates can run the tools and
# retriever without regenerating anything.
data/raw/
data/austin_raw_data.zip

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Editors / OS
.vscode/
.idea/
.DS_Store

# Task 7: the judge call log is append-only across runs and is regenerated by
# any evaluation run. Committing it would present verdicts from superseded
# runs beside current ones. Per-claim verdicts live in the results JSON.
evaluation/judge/results/judge_log.jsonl
