# Data: raw inputs are too large for GitHub (several files exceed the 100 MB
# hard limit) and everything under processed/ and index/ is regenerated by
# `python -m src.preprocessing.run_all` and `python -m src.rag.indexer`.
# See data/README.md for how to obtain the raw files.
data/*
!data/README.md

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Editors / OS
.vscode/
.idea/
.DS_Store
